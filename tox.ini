[tox]
envlist = py39
skipsdist = True
 
[testenv]
deps =
    -r{toxinidir}/requirements.txt
    pytest
    pytest-cov
commands = pytest --cov=api --cov-report=xml:{toxinidir}/coverage.xml --cov-branch